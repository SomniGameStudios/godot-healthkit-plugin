name: Build

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      godot_version:
        description: 'Godot version (e.g. 4.6.2)'
        required: false
        default: '4.6.2'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install scons
        run: pip install scons

      - name: Build plugin
        run: ./platforms/ios/scripts/build.sh ${{ github.event.inputs.godot_version || '4.6.2' }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HealthKitPlugin
          path: platforms/ios/build/output/
